{
  "name": "joi-config-generator",
  "version": "1.0.0-pre.4",
  "description": "Generates and validates JSON/dotenv configuration files using a JOI schema.",
  "main": "lib/index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "npm run test -- --watch",
    "build": "babel src --out-dir lib --copy-files",
    "build:watch": "npm run build -- --watch",
    "prepublishOnly": "npm run build && npm run test"
  },
  "author": "Guylian Cox <guyliancox@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": ">=8",
    "npm": ">=5"
  },
  "devDependencies": {
    "@foobarhq/eslint-config": "^1.2.0",
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-flow-strip-types": "^6.22.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "eslint": "^3.19.0",
    "flow-bin": "^0.74.0",
    "jest": "^20.0.4"
  },
  "dependencies": {
    "chalk": "^1.1.3",
    "constant-case": "^2.0.0",
    "dotenv-parser-serializer": "^1.0.1",
    "inquirer": "^6.0.0",
    "joi": "^13.4.0",
    "mz": "^2.7.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/madkingsag/joi-config-generator"
  }
}
